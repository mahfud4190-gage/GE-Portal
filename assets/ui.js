export const $=(s,r=document)=>r.querySelector(s);export const $$=(s,r=document)=>[...r.querySelectorAll(s)];
export function esc(v=''){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
export function toast(msg){const n=document.createElement('div');n.className='toast';n.textContent=msg;document.querySelector('#toast-root').append(n);setTimeout(()=>n.remove(),2800)}
export function modal({title,body,onSave,saveLabel='Simpan',wide=false}){const root=$('#modal-root');root.innerHTML=`<div class="modal-backdrop"><section class="modal" ${wide?'style="width:min(1100px,96vw)"':''}><header class="modal-head"><b>${esc(title)}</b><button class="x" data-close>×</button></header><div class="modal-body">${body}</div><footer class="modal-foot"><button class="btn secondary" data-close>Batal</button>${onSave?`<button class="btn" data-save>${esc(saveLabel)}</button>`:''}</footer></section></div>`;const close=()=>root.innerHTML='';$$('[data-close]',root).forEach(x=>x.onclick=close);$('.modal-backdrop',root).onclick=e=>{if(e.target===e.currentTarget)close()};const key=e=>{if(e.key==='Escape'){close();document.removeEventListener('keydown',key)}};document.addEventListener('keydown',key);if(onSave)$('[data-save]',root).onclick=async()=>{const b=$('[data-save]',root);b.disabled=true;try{await onSave(root);close()}catch(e){toast(e.message)}finally{b.disabled=false}};return root}
export function downloadCSV(name,rows){if(!rows.length)return toast('Tidak ada data untuk diunduh');const keys=[...new Set(rows.flatMap(Object.keys))].filter(k=>!['id'].includes(k));const q=v=>`"${String(v??'').replaceAll('"','""')}"`;const csv=[keys.map(q).join(','),...rows.map(r=>keys.map(k=>q(Array.isArray(r[k])?r[k].join('|'):r[k])).join(','))].join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download=name;a.click();URL.revokeObjectURL(a.href)}
export function parseCSV(text){const rows=[];let row=[],cell='',q=false;for(let i=0;i<text.length;i++){const c=text[i],n=text[i+1];if(q){if(c==='"'&&n==='"'){cell+='"';i++}else if(c==='"')q=false;else cell+=c}else if(c==='"')q=true;else if(c===','){row.push(cell);cell=''}else if(c==='\n'){row.push(cell);rows.push(row);row=[];cell=''}else if(c!=='\r')cell+=c}if(cell||row.length){row.push(cell);rows.push(row)}const h=(rows.shift()||[]).map(x=>x.trim());return rows.filter(r=>r.some(Boolean)).map(r=>Object.fromEntries(h.map((k,i)=>[k,r[i]??''])))}
